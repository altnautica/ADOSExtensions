{
  "name": "hello-plugin",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "pnpm -C gcs build",
    "test": "pnpm -C gcs test && cd agent && uv run pytest",
    "pack": "../../scripts/pack.sh ."
  }
}
