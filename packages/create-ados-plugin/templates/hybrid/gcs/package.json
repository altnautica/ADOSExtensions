{
  "name": "hello-plugin-gcs",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --format=esm --target=es2022 --loader:.json=json --outfile=plugin.bundle.js --minify",
    "test": "vitest run"
  },
  "dependencies": {
    "@altnautica/plugin-sdk": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "esbuild": "^0.24.0",
    "happy-dom": "^15.7.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
