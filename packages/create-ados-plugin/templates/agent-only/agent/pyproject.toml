[project]
name = "hello-agent-plugin"
version = "0.1.0"
description = "Skeleton agent-only ADOS plugin."
license = { text = "GPL-3.0-or-later" }
requires-python = ">=3.11"
dependencies = [
  "msgpack>=1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
